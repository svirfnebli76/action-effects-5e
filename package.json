{
  "name": "action-effects-5e",
  "version": "0.2.9",
  "private": true,
  "type": "module",
  "description": "Development utilities for the Action Effects 5E Foundry VTT module.",
  "scripts": {
    "check": "node scripts/dev/check-syntax.mjs",
    "test": "npm run check && node --test tests/*.test.mjs"
  }
}
